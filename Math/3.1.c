#include<stdio.h>
#include<math.h>
void main()
{
    int num;
    printf("enter an integer num: ");
    scanf("%d",&num);
    int output = abs(num);
    printf("the absolute value is = %d ",output);
}
